import { Cookie, Inter } from "next/font/google"

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
})


export const cookie = Cookie({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
})
